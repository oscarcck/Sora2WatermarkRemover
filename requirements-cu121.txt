# Requirements for Sora2WatermarkRemover - CUDA 12+ optimized
# Tested with CUDA 12.2 and Python 3.10 (matching Google Colab environment)
# Use this for maximum compatibility with Colab

# PyTorch with CUDA 12.1 support (compatible with CUDA 12.1-12.6 runtime)
# Note: PyTorch doesn't provide cu122 builds - cu121 works perfectly with CUDA 12.2+
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121
torchaudio==2.1.2+cu121

# Core ML/AI libraries - Updated for full compatibility
huggingface_hub==0.23.0
transformers==4.36.2
tokenizers==0.15.0
safetensors==0.4.1
diffusers==0.27.2
accelerate==0.25.0

# Computer Vision
opencv-python-headless==4.8.1.78
Pillow==10.4.0
scikit-image==0.22.0

# Inpainting - Latest stable version (matching Colab)
iopaint==1.6.0

# CLI tools
click==8.1.7
tqdm==4.66.1
loguru==0.7.2

# Configuration
PyYAML==6.0.1
omegaconf==2.3.0
yacs==0.1.8

# System monitoring
psutil==5.9.6

# GUI (optional - only needed for remwmgui.py)
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip==13.6.0

# Jupyter (optional - only needed for notebooks)
ipywidgets==8.1.1
ipykernel==6.27.1
jupyter==1.0.0
notebook==7.0.6
